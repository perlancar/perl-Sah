{
    "description": "Currently not yet checking prefilters.temp attribute",
    "tests": [
        {
            "name"           : "prefilters",
            "schema"         : ["str", {"prefilters":["Str::downcase"], "in":["a","b","c"]}],
            "valid_inputs"   : ["a", "A"],
            "invalid_inputs" : ["d"],
            "tags"           : ["clause:prefilters"]
        }
    ]
}
